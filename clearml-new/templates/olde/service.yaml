apiVersion: v1
kind: Service
metadata:
  name: {{ include "clearml-new.fullname" . }}
  labels:
    {{- include "clearml-new.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clearml-new.selectorLabels" . | nindent 4 }}
