apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clearml-new.fullname" . }}-test-connection"
  labels:
    {{- include "clearml-new.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clearml-new.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
